<!DOCTYPE html>

<html lang='en'>

  <head>
    <meta charset='utf-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title><%= page_title %></title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src='http://html5shim.googlecode.com/svn/trunk/html5.js' type='text/javascript'></script>
    <![endif]-->
    <%= stylesheet_link_tag 'application', :media => 'all' %>
    <link href='images/favicon.ico' rel='shortcut icon'>
    <link href='images/apple-touch-icon.png' rel='apple-touch-icon'>
    <link href='images/apple-touch-icon-72x72.png' rel='apple-touch-icon' sizes='72x72'>
    <link href='images/apple-touch-icon-114x114.png' rel='apple-touch-icon' sizes='114x114'>
  </head>
  
  <body>
    <% if session[:mode] == 'demo' %>
      <div class='navbar-fixed-top'>
    		<div class='pull-right'>
    	    <a href='#', onclick="switch_mode('normal')">
    	      <i class="icon-resize-full"></i>
    		    full app
    	    </a>
        </div>
      </div>
    <% else %>  

    <% end %>
    <div class='navbar navbar-fixed-top'>
      <div class='navbar-inner'>
        <div class='container-fluid'>
          <% if session[:mode] == 'sharable' %>
            <span class='brand'><%= page_title+"-"+page_subtitle %></span>
            <ul class='nav'>
              <li <%= "class='active'" if request.fullpath.include?('candidate') %>>
                <%= link_to 'Candidate', candidate_profile_public_path %>
              </li>
              <li <%= "class='active'" if request.fullpath.include?('recruiter') %>>
                <%= link_to 'Recruiter', recruiter_profile_path %>
              </li>
            </ul>
            <ul class="nav pull-right">
              <li>
                <a href='#', onclick="switch_mode('normal')">
                  <i class="icon-resize-small icon-white"></i>
                  full app
                </a>
              </li>
            </ul>
          <% else %>
            <%= link_to 'Engaccino', root_path, :class => 'brand' %>
            <ul class='nav pull-right'>
              <li class='dropdown'>
                <a href='#' class='dropdown-toggle' data-toggle='dropdown'>protos<b class='caret'></b></a>
                <ul class='dropdown-menu'>  
                  <li>
                    <a href='#', onclick="switch_mode('sharable')">sharable profile</a>
                  </li>
                </ul>
              </li>
            </ul>
          <% end %>
        </div>
      </div>
    </div>

    <div class='container-fluid'>
      <%= yield %>
    </div>

    <footer>
    </footer>

    <%= javascript_include_tag 'application' %>

  </body>
  
</html>
